#!/usr/bin/env bash

docker run --rm -v "$(pwd)":/code \
  --mount type=volume,source="$(basename "$(pwd)")_cache",target=/code/target \
  --mount type=bind,source="$(realpath ../pamp)",target=/pamp \
  cosmwasm/rust-optimizer:0.15.1
